[pytest]
# Sử dụng đường dẫn tương đối 'tests' để tránh nhầm lẫn khi chạy từ bên trong thư mục backend.
# Trước đây: backend/tests (dẫn tới backend/backend/tests khi rootdir=backend)
testpaths = tests
addopts = -q
